# @import(../../apps/server/, pick=[NODE_ENV, DATABASE_URL, CORS_ORIGIN, OPENROUTER_API_KEY, OPENROUTER_MODEL, SUPABASE_URL, SUPABASE_JWKS_URL, SUPABASE_JWT_SECRET, R2_BUCKET_NAME, R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY])
# @defaultRequired=false
# @defaultSensitive=true
# ---
ALCHEMY_PASSWORD=
